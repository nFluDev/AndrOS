<?xml version="1.0" encoding="utf-8"?>
<!-- Kumanda: telefon Mac'in dokunmatik yüzeyi ve klavyesi.
     Görüntü YOK — bu ekran yansıtmanın tersi. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/pageControl"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:paddingStart="16dp"
    android:paddingEnd="16dp"
    android:paddingTop="20dp"
    android:paddingBottom="12dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:orientation="horizontal">

        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="@string/tab_control"
            android:textColor="@color/text"
            android:textSize="22sp"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/controlStatus"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="@drawable/pill_bg"
            android:paddingStart="12dp"
            android:paddingEnd="12dp"
            android:paddingTop="6dp"
            android:paddingBottom="6dp"
            android:textColor="@color/accent"
            android:textSize="12sp" />
    </LinearLayout>

    <!-- Dokunmatik yüzey -->
    <dev.naer.andros.feature.TrackpadView
        android:id="@+id/trackpad"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:layout_marginTop="14dp"
        android:background="@drawable/card_bg" />

    <TextView
        android:id="@+id/controlHint"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"
        android:text="@string/control_hint"
        android:textColor="@color/text_dim"
        android:textSize="11sp" />

    <!-- Tuşlar -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="12dp"
        android:orientation="horizontal">

        <Button
            android:id="@+id/leftClick"
            style="@style/Widget.Material3.Button.TonalButton"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="@string/left_click" />

        <Button
            android:id="@+id/rightClick"
            style="@style/Widget.Material3.Button.TonalButton"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="8dp"
            android:layout_weight="1"
            android:text="@string/right_click" />

        <Button
            android:id="@+id/keyboardButton"
            style="@style/Widget.Material3.Button"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="8dp"
            android:layout_weight="1"
            android:text="@string/keyboard" />
    </LinearLayout>

    <!-- Klavye giriş kutusu: açılıp kapanıyor.
         Görünmez bir alan yerine GERÇEK bir kutu: kullanıcı ne yazdığını
         görmeden yazmak zorunda kalmasın. -->
    <EditText
        android:id="@+id/keyInput"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"
        android:background="@drawable/code_bg"
        android:hint="@string/keyboard_hint"
        android:imeOptions="actionGo|flagNoExtractUi"
        android:inputType="textNoSuggestions|textVisiblePassword"
        android:padding="14dp"
        android:textColor="@color/text"
        android:textColorHint="@color/text_dim"
        android:textSize="15sp"
        android:visibility="gone"
        android:autofillHints="" />
</LinearLayout>
